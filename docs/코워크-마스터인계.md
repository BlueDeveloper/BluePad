# 코워크 마스터 인계 — BluePad 운영 자동화 (SEO·GA4·모니터링·마케팅)

> **BluePad(bluepad.work) 운영 자동화의 최상위 인덱스.** 이 한 파일로 진입 → 하위는 `CLAUDE.md`·`docs/work-log/`.
> **운영 주체: Archive(cowork) 세션이 BluePad도 겸임** (2026-06-22). 개발 세션은 코드/앱/기능만.
> ⚠ 시크릿(토큰)은 **경로만** 기재. 값은 파일에서 읽어 쓴다(평문 금지).
> 제품: 상용 마크다운 에디터(Typora 대안), Tauri/Windows MSI, Freemium $10.99 PayPal. **전환 = 앱 다운로드**.

---

## ★ 보고 포맷 (공통 — 모든 스케줄 작업·BLUE 보고 공통)
모든 점검·보고는 아래 **5항목만**, 간략하게(각 1~2줄, 지난 점검 대비 변화 중심, 해당 없으면 "없음"). 장황한 결과 나열 금지 — 상세는 `docs\work-log\`에만.
- 📈 좋아진 점
- 📉 나빠진 점
- 🔧 수정할 점
- 🚨 긴급 고칠 점
- 💡 개선 대안 (더 나아지기 위한 구체 제안 1~3개, **항상 제시**)

**데드링크 정책**: 404·삭제 라우트는 **낮은 우선순위**(평소 '수정할 점' 한 줄, 급증 시만). 각 스케줄 작업은 이 섹션을 참조한다. **포맷 변경은 여기 한 곳만** 고치면 된다.
**자가점검 (사람 승인 불필요)**: 보고·발행·저장 전 결과를 스스로 1회 검증한다 — 수치는 이상치 재확인, 콘텐츠는 품질·사실 체크리스트 대조. 통과 시 진행, 미달이면 보류·플래그한다(검수 게이트로 멈추지 않는다).

## 0. 자동화 작업 (스케줄러)
| taskId | 주기 | 내용 |
|---|---|---|
| `bluepad-daily-index-request` | 매일 10:45 | bluepad.work 미색인 ~10건 GSC 색인요청 |
| `bluepad-weekly-check` | 월 09:50 | GA4·CF·GSC·Clarity 주간 모니터링 보고 |
| `bluepad-blog-post` | 주 3회(월·수·금) | 영문 블로그 **완전자동 발행**: 초안→정적 HTML→index·sitemap 갱신→push→배포 |
| `ga4-filedownload-keyevent-check` | 일회성 6/23 | file_download(다운로드)를 GA 주요이벤트(전환)로 표시 |

> **블로그 완전자동 (2026-06-23 BLUE 확정, 배포구조 정정)**: 자동화 샌드박스는 **파일 삭제 불가(git 불가)+외부망 차단(API 불가)** → 배포는 **호스트가 담당**. 흐름: ① `bluepad-blog-post`(샌드박스)가 정적 HTML 생성+index 카드+sitemap URL 추가(**파일 쓰기만, git 금지**) → ② **호스트 Windows 작업 스케줄러가 `scripts/deploy-blog.ps1` 실행**(landing/ 커밋·푸시) → Cloudflare Pages 자동배포. 정적 파일이라 빌드 없음, 깨져도 해당 1페이지만 영향. **사람 개입 없음**(호스트 작업 스케줄러 1회 등록 후).

## 1. 자격·ID (값 아님 — 경로/식별자만)
- **GA4**: 측정 `G-3PRY6YKV05` / 속성 `542487056` / 계정 BRP_archive(`395213120`) / 스트림 `15125433851`. GA↔GSC 연결됨(6/22).
- **GSC**: `sc-domain:bluepad.work` (도메인 속성). 네이버 메타 `9b479417f022befdffd0b837c015469b604758d3`.
- **Cloudflare**: zone `027e709c72befdd4cd39c8ede1a9df8a` / Pages(랜딩) / Workers 3종(license-api·download·checkout) / D1 `6e9776a9-f68a-45b9-b3f7-00cbd6bda70c` / R2 `bluepad-downloads`.
- **Clarity**: `wkokfmayny`.
- **IndexNow**: `deploy.sh` 8단계가 Bing/Naver/Yandex 제출.
- **결제**: PayPal Live $10.99 / **전환 = 다운로드(file_download)**.

## 2. 콘텐츠·SEO (랜딩 bluepad.work)
- 랜딩: ko/en/ja 3언어 + 블로그 7편×3언어(정적 HTML, BlogPosting+Organization 스키마) + legal.
- 구조화데이터: index에 SoftwareApplication+Offer+Organization+FAQPage. llms.txt 보유. sitemap 45 URL.
- robots: **(a) 인용봇 허용·학습봇 차단**(Archive·Memoria와 동일, 2026-06-22).
- **SEO 푸시**: 글로벌(영문) 마크다운 에디터 롱테일 콘텐츠 확장 — `bluepad-blog-post` 작업이 주 3회 **초안→정적 HTML→배포까지 무인 발행**(§0 완전자동 참조).

## 3. 경계 (이 운영이 안 하는 것)
- 앱 코드/빌드/배포(deploy.sh)·라이선스·결제 로직 = 개발 세션.
- 시크릿 값 노출·커밋 금지. 코드 대규모 변경은 BLUE 확인.
