# 2026-06-12 — GitHub Actions 저장공간 쿼터 100% 사고 처리 (ROOT)

## 사고
- GitHub 메일: Actions storage 0.5GB/월 100% 사용 경고.
- 원인: **Linux Build 워크플로우**가 태그 푸시(v1.16.0/v1.16.1 등)마다 91MB artifact 업로드 → 4개 366MB 점유(계정 전체의 99.9%).
- 같은 산출물이 이미 R2 + GitHub Release 자산으로 영구 보관 중 — artifact는 순수 중복이었음.

## 조치
1. 아티팩트 4개 전부 삭제(366MB 회수, API).
2. `linux-build.yml` — artifact 업로드 스텝에 `if: github.event_name == 'workflow_dispatch'` 추가(`8b1d1e9`). **태그 릴리즈 시 저장공간 소모 0**, 수동 디버그 실행 시에만 91MB(보관 1일).
3. 전 레포(17) 워크플로우 전수 스캔 — 그 외 upload-artifact는 hlmobile/castlink 백업(KB급)뿐, 무해 확인.

## 알아둘 것
- 과금 위험 없음: Actions $0 예산이면 초과 시 차단만 됨(관리자 Budgets 확인 권장). 쿼터는 7/1 리셋이나 삭제로 이미 해소.
- 릴리즈 플로우 영향 없음: MSI(로컬)·Linux(R2+Release) 배포 채널은 이 쿼터와 무관.
