# 작업로그 2026-06-26

## 영문 블로그 신규 1편 — Mermaid 다이어그램 가이드 (주 3회 자동 발행)

### 작업 내역
- 신규 글 생성: `landing/en/blog/markdown-diagrams-with-mermaid.html`
  - 주제: "How to Make Diagrams in Markdown with Mermaid (Beginner's Guide)"
  - 키워드(롱테일, KD<30 추정): markdown diagrams, mermaid markdown, flowchart in markdown, diagrams as code, mermaid sequence diagram
  - 기존 9편과 주제 중복 없음(기존: 에디터 비교/Typora 대안/PDF 내보내기/포커스모드/경량노트앱/마크다운입문/마크다운팁/회의록/라이팅모드). Mermaid 다이어그램은 신규 영역 — BluePad 무료 기능(Mermaid·KaTeX)과 직접 연결.
- 템플릿: `typora-free-alternative.html` 복사 후 교체. head 보일러플레이트(Clarity `wkokfmayny`·GA4 `G-3PRY6YKV05`·naver 메타·Pretendard·스타일) 그대로 유지.
- 교체 항목: title·description·keywords·canonical·OG·twitter·BlogPosting JSON-LD·본문(H2 7개/H3·표 1개·리스트·코드 예시 2개·FAQ 3개)·하단 다운로드 CTA.
- 영문 전용: hreflang ko/ja 줄 삭제, `<html lang="en">`·canonical·hreflang en·x-default만 자기 자신. nav 언어 스위처도 제거(EN 단독).
- 내부링크 4개: 마크다운 입문 가이드, 마크다운 팁, PDF 내보내기, bluepad.work 홈.

### 인덱스·사이트맵
- `landing/en/blog/index.html`: 최상단에 새 카드 추가(Guide 태그, June 26 2026).
- `landing/sitemap.xml`: 신규 url 추가(loc/lastmod 2026-06-26/changefreq monthly/priority 0.6).

### 자가점검(통과)
- canonical/OG/JSON-LD URL이 모두 slug `markdown-diagrams-with-mermaid`와 일치(5건).
- hreflang en + x-default만 존재(ko/ja 없음).
- GA4·Clarity·naver 스니펫 보존 확인.
- 태그 밸런스: h2 7/7, article 1/1, html/body 닫힘 정상.
- CTA `/en/download/` 존재.

### ⚠ 발견·수정한 기존 이슈
- `landing/sitemap.xml`이 **기존부터 끝부분 잘림(truncated)** — 마지막 `/support/` 항목이 `<priority>0.3</`에서 끊기고 `</urlset>` 누락 → XML 파싱 실패 상태였음. (이번 신규 url 추가는 상단부라 무관)
- 누락 닫는 태그만 append하여 복구(`priority>` + `</url>` + `</urlset>`). 삭제 없이 추가만. `xml.dom.minidom` 파싱 통과·url 47개 정상.

### 사실 가드(§11)
- Mermaid 사실은 WebSearch로 확인 후 기술(오픈소스 JS 기반, flowchart TD/LR·노드 셰이프·`-->`·sequenceDiagram `->>`/`-->>`). 타사 가격/라이선스 추측 없음. "Mermaid는 오픈소스라 엔진 자체는 무료"만 단정.

### 다음
- 호스트 작업 스케줄러 `scripts/deploy-blog.ps1`가 커밋·푸시·배포 처리(이 세션은 파일 생성/수정까지만).
- 다음 발행: 월요일(주 3회 월·수·금).

---

## 일일 색인요청 (bluepad-daily-index-request)

GSC `sc-domain:bluepad.work` 도메인 속성 점검 (Claude in Chrome, 로그인 세션 정상).
색인 현황: **생성됨 28 / 안됨 20** (전일과 동일, 데이터 최종 업데이트 26.6.12 — 갱신 없음).

### 미색인 20건 사유별 (전일과 동일)
- 리디렉션이 포함된 페이지 2 → 제외(리디렉션)
- 크롤링됨-현재 색인 안됨 1 → 후보였으나 어제(6/25)+6/23 요청한 동일 URL → 제외
- 적절한 표준 태그가 포함된 대체 페이지 8 → 제외(canonical 대체, 정상)
- NOINDEX 태그 제외 5 → 제외(의도된 noindex)
- 중복 페이지(Google 다른 표준 선택) 1 → 제외(낮은 우선순위)
- 리디렉션 오류 3 → 제외(데드/오류)
- 발견됨-현재 색인 안됨 0 → 없음

### 색인 생성 요청 (0건)
- 유일한 유효 후보 `https://bluepad.work/ko/blog/best-markdown-editors-2026.html`(크롤링됨-색인안됨, 최종크롤 2026.5.9)는 **어제(6/25)와 6/23에 이미 요청한 URL** → 정책상 "어제 요청분 제외" 적용하여 **오늘은 요청 안 함**.
- 데이터가 26.6.12 이후 미갱신이라 기존 요청 효과가 아직 보고서에 반영되지 않음(상태 변화 대기). 발견됨-색인안됨 0건이라 신규 후보 없음.
- 할당량: 0/10 사용. 억지로 채우지 않음(유효 신규 후보 없음).

### 사이트맵/이상 점검
- sitemap `https://bluepad.work/sitemap.xml`: 상태 **성공**, 발견 페이지 42, 마지막 읽은 날짜 2026.5.26. "가져올 수 없음" 없음.
- 색인 수 28건 — 전일 대비 급락 없음(안정).
- 비고: 오늘 블로그 작업으로 로컬 sitemap.xml은 47 URL(끝부분 잘림 복구 포함)이나, GSC 발견은 42(마지막 읽음 5.26). 호스트 deploy-blog.ps1 푸시·배포 후 GSC 재페치되면 반영 예상 — 추적만.
