# 작업로그 2026-06-23

## 일일 색인요청 (bluepad-daily-index-request)

GSC `sc-domain:bluepad.work` 도메인 속성 점검. 색인 현황: 생성됨 28 / 안됨 20.

미색인 20건 사유별:
- 리디렉션이 포함된 페이지 2 → 제외(리디렉션)
- 크롤링됨-현재 색인 안됨 1 → **요청 대상**
- 적절한 표준 태그가 포함된 대체 페이지 8 → 제외(canonical 대체, 정상)
- NOINDEX 태그 제외 5 → 제외(의도된 noindex)
- 중복 페이지(Google 다른 표준 선택) 1 → 제외(낮은 우선순위)
- 리디렉션 오류 3 → 제외(데드/오류)
- 발견됨-현재 색인 안됨 0 → 없음

### 색인 생성 요청 (1건)
| URL | 사유 | 결과 |
|---|---|---|
| https://bluepad.work/ko/blog/best-markdown-editors-2026.html | 크롤링됨-색인안됨 (최종크롤 5/9) | 색인 생성 요청됨 — 우선순위 크롤 대기열 추가 |

할당량: 미소진(1/10 사용). 유효 미색인 후보가 1건뿐이라 1건만 요청. 나머지 19건은 정책상 자동 제외 대상(noindex·리디렉션·canonical대체·중복).

---

## 영문 블로그 자동 발행 (bluepad-blog-post) — 주 3회 월·수·금

**주제**: How to Export Markdown to PDF on Windows (4 Easy Ways)
**slug**: `export-markdown-to-pdf-windows`  /  태그: Guide  /  6 min read

### 1. 키워드·중복 점검
- 롱테일 `export markdown to pdf windows` (전환=다운로드, BluePad PDF/HTML 내보내기=Pro 기능 연결).
- 기존 7편(best-markdown-editors-2026 / typora-free-alternative / focus-mode / lightweight-note-app-windows / markdown-editor-for-developers / markdown-writing-tips / writing-mode)과 **주제 비중복** 확인.

### 2. 사실 가드 (CLAUDE.md §11)
- WebSearch로 Pandoc=LaTeX 엔진 기본 사용, TeX 배포판(TeX Live/TinyTeX) 필요, `pandoc file.md -o file.pdf`, "pdflatex not found" 오류 확인 후 기술.
- BluePad는 사실대로: PDF/HTML 내보내기는 **Pro 기능**(무료 트라이얼 중 사용 가능)으로 정확히 표기.

### 3~5. HTML 생성·인덱스·사이트맵·검증 (모두 완료)
- `landing/en/blog/export-markdown-to-pdf-windows.html` 생성(typora 템플릿 복사). head 보일러플레이트(Clarity wkokfmayny·GA4 G-3PRY6YKV05·naver 메타·Pretendard·스타일) 보존. title·desc·keywords·canonical·OG·twitter·BlogPosting JSON-LD(datePublished/Modified=2026-06-23) 교체. 본문 H2/H3 + 비교표 + 리스트 + 코드예시 + FAQ 3개 + 하단 다운로드 CTA(`/en/download/`).
- ⚠ hreflang ko/ja 줄 **삭제**(영문 전용글) — en·x-default만 자기 자신.
- `index.html` 카드 1개 추가(최상단), `sitemap.xml` url 1개 추가(lastmod 2026-06-23, priority 0.6).
- 검증: 태그 밸런스 OK, 필수 스니펫(GA4·Clarity·naver·canonical·JSON-LD) 보존 확인, sitemap XML well-formed 확인.

### 6. 배포 — ⚠ 미완(환경 제약)
- **git commit/push 실패**: cowork 샌드박스 마운트의 `.git` 디렉터리가 **읽기 전용**(`.git/HEAD.lock`·objects unlink "Operation not permitted") + GitHub 자격증명 없음(HTTPS remote, `~/.git-credentials` 없음). 1회 재시도 후에도 동일 → 정책대로 보고 후 종료.
- **파일 자체는 사용자 실디스크에 정상 생성·수정 완료**(3개 파일 모두 완전·검증 통과). **Windows 측에서 commit·push만 수행하면 됨**:
  ```
  cd C:\BLUE\Project\blue\SAAS\BluePad
  git add landing/en/blog/export-markdown-to-pdf-windows.html landing/en/blog/index.html landing/sitemap.xml
  git commit -m "blog: How to Export Markdown to PDF on Windows (4 Easy Ways)"
  git push
  ```
- push 후 Cloudflare Pages 자동배포 → `https://bluepad.work/en/blog/export-markdown-to-pdf-windows` 200 확인 + IndexNow 핑(다음 deploy.sh 릴리스에 일괄 제출 가능).

### 미해결 이슈
- cowork 샌드박스에서 BluePad 레포 git 쓰기 불가(.git 읽기전용·자격증명 부재) → **블로그 자동발행의 배포 단계가 무인화 불가**. 후속: ① Windows 측 git push 위임 경로 마련, 또는 ② 자격증명/쓰기권한 있는 실행 환경에서 cron 구동 필요.
