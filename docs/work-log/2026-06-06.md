# 2026-06-06 작업 로그

> 기간: 2026-06-04 ~ 06-06 세션. 배포 v1.14.9 → v1.15.2.

## 작업 내역

### 라이선스
- 본인용 Pro 라이선스 D1 직접 발급: `BP-8ZBM-3P7Q-ZY8V-EX23` (email blueehdwp@gmail.com, max_devices 10, live, active). 앱에서 1회 입력해 활성화 완료(status=pro 확인).

### 버그 수정 / 배포
- **v1.14.9 — 시작 시 흰 화면(렌더 실패) 근본 원인 수정**
  - 원인: 자동 복원되는 활성 탭의 파일이 삭제/이동돼 사라짐 → `useFileManager` 복원 catch가 마지막 탭을 제거해 `tabs=[]` → `activeTab` undefined → `activeTab.content` 접근에서 React 렌더 전체 크래시 = 블랭크.
  - 진단: 빌드 클린·WebView2 148 정상·GPU 무관 확인 후, `WEBVIEW2_ADDITIONAL_BROWSER_ARGUMENTS=--remote-debugging-port=9222` + Playwright CDP로 실제 WebView2 콘솔/localStorage 확보해 확정. (기법은 메모리 [[webview2-cdp-debug]])
  - 수정: 복원 catch에서 마지막 탭이면 `createTab()`로 대체 + `activeTab` 최후 가드. CSP `connect-src`에 `ipc: http://ipc.localhost` 추가(Tauri v2 권장).
- **v1.15.0 — 새 탭 UX + 작가모드 맞춤법**
  - 탭 `+` 버튼을 마지막 탭 옆(`.tabbar-tabs` 내부)으로 이동 + 클릭 시 파일형식(MD/TXT/HTML/CSS/JS/JSON/YAML) 드롭다운(`position:fixed`로 overflow 회피). 선택 시 `Untitled.확장자` 탭 생성.
  - `Ctrl+Shift+N` 새 탭 단축키.
  - 작가모드일 때만 Milkdown `.ProseMirror` spellcheck on(평상시 off). SourceEditor는 원래 off.
  - 곁가지: `+` 툴팁 "Ctrl+N"→"Ctrl+Shift+N" ko/en/ja 정정.
- **v1.15.1 — 한글 IME 한 박자 지연 수정**
  - SourceEditor(소스모드/.txt textarea)를 controlled→uncontrolled(defaultValue)+composition 처리로 변경. 조합 중 부모 상태 재할당으로 조합 끊기던 현상 제거.
- **v1.15.2 — 코드 에디터 멀티커서**
  - CodeMirror: `allowMultipleSelections`+`drawSelection`+`rectangularSelection`+`crosshairCursor` 활성화. Alt+클릭, Ctrl+D(다음 동일 단어), Ctrl+Alt+↑/↓(위·아래 커서), Alt+드래그(열 선택). 키맵은 기존 search/default keymap에 이미 존재.

### 분석 / 검토 (배포 아님)
- **SEO 표준 대조**: 라이브 SEO가 표준(글로벌 인프라 §6) 충족/초과(hreflang·JSON-LD·IndexNow). 경미 갭만 → 아래 미해결.
- **다운로드 실유저 분석**: "Win 18"은 전부 데이터센터/봇(GCP 34.83.x, Cloudflare 104.28.x, 2a06:98c0:: UA=unknown). OS↔파일타입 매칭 + 데이터센터 IP 제외 시 **윈도우 실유저 0건**. → 유입(발견) 문제로 진단.
- **잠재 오류 검토**: 아래 미해결 이슈로 정리.

## 미해결 이슈

### 안정성 (권장 1.16.0)
- [ ] **React ErrorBoundary + 전역 에러 핸들러 부재** — 렌더 예외 1건이 앱 전체 흰 화면으로 번짐(이번 1.14.9 블랭크의 근본 증폭 원인). 에디터 감싸는 ErrorBoundary + 복구 UI + `window.onerror`/`unhandledrejection` 필요.
- [ ] **writeTextFile 6곳 try/catch 부재** (saveFile/saveFileAs/closeTab저장/autosave/saveAllModified) — 저장 실패가 조용히 묻힘 = 데이터 유실 위험. showToast로 에러 노출 필요.
- [ ] App.tsx `recentFiles`(:129) `Array.isArray` 검증 누락(형제 코드는 검증함).
- [ ] `get_cli_file_path` invoke에 `.catch()` 부재 → IPC 실패 시 unhandled rejection.
- [ ] 트라이얼 timestamp NaN 방어 검증 필요(`syncTrialWithServer`가 서버 값 파싱 시 NaN→"D-NaN" 가능성).

### SEO / 배포 인프라
- [ ] **GitHub Release가 deploy.sh [8/8]에서 v1.11.2(2026-05-19) 이후 계속 누락** — 자동 업데이트엔 무관(R2 update.json 사용)하나 원인 점검 필요.
- [ ] 레포 미추적 에셋 3개: `og-image.png`, `apple-touch-icon.png`, `favicon-512.png` — 라이브 200이나 git 미추적 → Pages 레포 재빌드 시 404 위험. 커밋 필요.
- [ ] CLAUDE.md sitemap 수치 드리프트(42 표기 vs 라이브 45) 정정.

### 제품 / 그로스
- [ ] **한글 IME 지연이 WYSIWYG(Milkdown)에서도 나는지 사용자 확인 대기** — 1.15.1은 SourceEditor(소스/.txt)만 수정. WYSIWYG면 별도 대응(ProseMirror/postRender DOM 변형 검토).
- [ ] **윈도우 실유저 다운로드 0건 = 유입 문제** — AlternativeTo 등재/Show HN/Product Hunt 등 오프사이트 유통 필요. SC `webmasters.readonly` 재인증 시 노출/클릭으로 "발견 0 vs 전환 0" 확정 가능(`gcloud auth application-default login --scopes=...webmasters.readonly`).
