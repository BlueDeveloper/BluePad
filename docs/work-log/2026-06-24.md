# 작업로그 2026-06-24 — BluePad 영문 블로그 자동 발행

## 작업: 신규 영문 블로그 1편 작성 (주 3회 / 수요일분)

### 키워드·주제 선정
- **주제**: "How to Take Meeting Notes in Markdown (A Simple System)"
- **slug**: `meeting-notes-in-markdown`
- **니치**: note app / productivity (롱테일, 저경쟁)
- **타깃 키워드**: meeting notes in markdown, markdown meeting notes template, how to take meeting notes, markdown note-taking system, action items markdown
- **주제 중복 점검**: 기존 8편(best-markdown-editors / export-pdf / focus-mode / lightweight-note-app / why-use-markdown / markdown-tips / typora-alternative / writing-mode) 과 비교 — 회의록 작성 시스템·템플릿은 미커버 영역, 중복 없음.
- **사실 가드(§11)**: 타사 제품·가격·라이선스 주장 없음. BluePad 기능 서술은 CLAUDE.md 기준(네이티브 Windows·라이브 프리뷰·탭·무료 일상사용)만 사용.

### 생성·수정 파일
1. `landing/en/blog/meeting-notes-in-markdown.html` (신규)
   - 템플릿: `export-markdown-to-pdf-windows.html`(영문 전용본) 복사 후 교체.
   - head 보일러플레이트(Clarity wkokfmayny·GA4 G-3PRY6YKV05·naver 메타·Pretendard·스타일) 보존.
   - `<html lang="en">`, hreflang en/x-default + canonical만 자기 자신. ko/ja hreflang 없음(영문 전용).
   - 본문: H2/H3 구조 + 코드블록(템플릿/액션아이템) + 리스트 + FAQ 3개 + 다운로드 CTA. 내부링크 2개(why-use-markdown, lightweight-note-app) + 홈/다운로드.
2. `landing/en/blog/index.html` — 최상단에 신규 카드 추가(Productivity 태그).
3. `landing/sitemap.xml` — `/en/blog/meeting-notes-in-markdown` url 엔트리 추가(priority 0.6).

### 검증
- slug URL 일관성: canonical/og/twitter/JSON-LD/hreflang 5곳 모두 `meeting-notes-in-markdown` 일치.
- GA4·Clarity·naver 스니펫 보존 확인.
- 태그 밸런스(html/head/body/article/footer) 및 html.parser 파싱 — unclosed/stray 0.
- sitemap.xml XML 유효성 검증 통과.

### 미해결/주의
- ⚠ **sitemap.xml 기존 손상 복구함**: 작업 전 sitemap.xml 말단이 `<priority>0.3</prior`에서 잘려 있었음(마지막 support/ url의 `</priority></url>`·`</urlset>` 누락). 내 엔트리 삽입(라인 111)과 무관한 기존 truncation. XML 파싱 실패 방지 위해 말단 닫힘태그 보강(`</priority></url></urlset>`)함. → **호스트 배포 전 sitemap 생성 스크립트가 truncate하는지 점검 권장**.
- 배포 대기: 자동화 샌드박스는 git/외부망 차단 → 파일 생성까지만. 커밋·푸시는 호스트 `scripts/deploy-blog.ps1` 소관.

**landing/en/blog/meeting-notes-in-markdown.html 생성 완료 — 호스트 deploy-blog.ps1 배포 대기**

---

## 작업: GA4 file_download 주요 이벤트(전환) 재확인 (ga4-filedownload-keyevent-recheck)

6/23 `ga4-filedownload-keyevent-check` 실행 후 별표 성공 여부가 로그에 안 남아 미확인 → Claude in Chrome으로 GA4(속성 542487056) 직접 재확인.

### 확인 결과
- **관리 > 이벤트(주요 이벤트) 목록** (`/admin/events/hub`)
  - `file_download` — **별표(★) 채워짐 = 주요 이벤트(전환)로 정상 표시됨**. 스트림 bluepad.work 지난 28일 활성.
  - `purchase` — 별표(☆) 비어 있음 = 주요 이벤트 아님. (스트림 데이터 감지 안 됨)
- **집계 확인** (홈 대시보드, 지난 7일): **주요 이벤트 = 1**. 주요 이벤트로 표시된 건 file_download 단독 → 이 1건이 file_download 전환. **별표 정상 + 집계 시작됨(전환 카운트 1+)**.

### 6/23 작업 결론
- 6/23 `ga4-filedownload-keyevent-check`의 별표 처리는 **성공**으로 확정. file_download 전환 집계 정상 가동 중.

### 미결 상기 (이번엔 보고만, 처리 안 함)
- 내부 트래픽 제외: 공인 IP `58.233.164.197` 수동 등록 **미완** — 내부 다운로드가 전환에 섞일 수 있음.
- `purchase` 전환: 별표 미표시 + 스트림 데이터 없음. 결제(PayPal) 0건 상태와 일치. 작업지시서 적용 여부 별도 확인 필요.

**file_download 별표 정상·집계 시작 확정. purchase는 미표시(결제 0건과 일치).**

---

## 일일 색인요청 (bluepad-daily-index-request)

GSC `sc-domain:bluepad.work` 도메인 속성 점검. 색인 현황: **생성됨 28 / 안됨 20** (전일과 동일, 데이터 최종 업데이트 26.6.12).

미색인 20건 사유별:
- 리디렉션이 포함된 페이지 2 → 제외(리디렉션)
- 크롤링됨-현재 색인 안됨 1 → 요청 후보이나 **어제(6/23) 이미 요청** → 제외
- 적절한 표준 태그가 포함된 대체 페이지 8 → 제외(canonical 대체, 정상)
- NOINDEX 태그 제외 5 → 제외(의도된 noindex)
- 중복 페이지(Google 다른 표준 선택) 1 → 제외(낮은 우선순위)
- 리디렉션 오류 3 → 제외(데드/오류)
- 발견됨-현재 색인 안됨 0 → 없음

### 색인 생성 요청 (0건)
유효 미색인 후보가 `https://bluepad.work/ko/blog/best-markdown-editors-2026.html`(크롤링됨-색인안됨, 최종크롤 5/9) **단 1건**인데, 이는 어제(6/23) 이미 색인요청하여 우선순위 크롤 대기열에 추가된 URL. 정책상 어제 요청분은 재요청 금지(할당량 낭비 방지) → **오늘 요청 0건**. 발견됨-색인안됨은 0건이라 신규 후보 없음.

할당량: 미소진(0/10 사용).

### 이상 점검
- 사이트맵 `https://bluepad.work/sitemap.xml`: 상태 **성공**, 발견된 페이지 42, 마지막 읽음 2026.5.26. "가져올 수 없음" 아님 — 정상.
- 색인 수: 28건, 전일 대비 변화 없음(급락 없음).
- ⚠ 참고: 사이트맵 마지막 읽음이 5/26로, 이후 추가된 블로그가 GSC 사이트맵에 미반영 가능성. 호스트 배포 후 사이트맵 재제출/재읽기 유도 검토.
