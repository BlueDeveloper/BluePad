# 작업로그 2026-06-27 — BluePad

## 일일 색인요청 (bluepad-daily-index-request)

GSC `sc-domain:bluepad.work` 도메인 속성 점검 (Claude in Chrome, 로그인 세션 정상).
색인 현황: **생성됨 28 / 안됨 20** (전일과 동일, 페이지 보고서 데이터 최종 업데이트 26.6.12 — 갱신 없음).

### 미색인 20건 사유별 (전일과 동일, 7개 사유)
- 리디렉션이 포함된 페이지 2 → 제외(리디렉션)
- 크롤링됨-현재 색인 안됨 1 → 후보였으나 **라이브 검사 결과 리디렉션 오류로 재분류** → 제외(아래 ★)
- 적절한 표준 태그가 포함된 대체 페이지 8 → 제외(canonical 대체, 정상)
- NOINDEX 태그 제외 5 → 제외(의도된 noindex)
- 중복 페이지(Google 다른 표준 선택) 1 → 제외(낮은 우선순위)
- 리디렉션 오류 3 → 제외(데드/오류)
- 발견됨-현재 색인 안됨 0 → 없음

### 색인 생성 요청 (0건)
- 유일한 보고서 후보 `https://bluepad.work/ko/blog/best-markdown-editors-2026.html`(페이지 보고서상 "크롤링됨-색인안됨", 6.23·6.25 각 1회 요청 이력) 를 **라이브 URL 검사**한 결과:
  - 상태: "URL이 Google에 등록되어 있지 않음"
  - **페이지 색인 생성: 리디렉션 오류** / **페이지 가져오기: 실패 — 리디렉션 오류**
  - 최근 크롤링 **2026.6.25 10:51:52** (Googlebot 스마트폰), 크롤링 허용 예, 색인 생성 허용 여부 해당없음
  - 참조 페이지: `https://bluepad.work/blog/best-markdown-editors-2026`, `https://bluepad.work/ko/blog/`
- ★ **판단: 색인요청 안 함.** 페이지 보고서(6.12 데이터)는 "크롤링됨-색인안됨"으로 표시하나, 라이브 검사(6.25 크롤)는 **Googlebot 페이지 가져오기가 리디렉션 오류로 실패**함을 보여줌. 정책상 리디렉션 오류는 자동 제외 대상이며, 색인요청해도 가져오기 자체가 실패하므로 무의미. 이 페이지가 계속 미색인인 **근본 원인 = 리디렉션 오류**(단순 색인 지연 아님).
- 할당량: **0/10 사용**. 유효 신규 후보 없음(억지로 채우지 않음).
- 보조 진단: 샌드박스 curl로 `/ko/blog/best-markdown-editors-2026.html` 및 `/blog/best-markdown-editors-2026` 모두 403(Cloudflare 봇 차단)으로 리디렉션 체인 직접 확인 불가 → GSC 라이브 검사를 정본으로 채택.

### 사이트맵/이상 점검
- sitemap `https://bluepad.work/sitemap.xml`: 상태 **성공**, 발견 페이지 42, 마지막 읽은 날짜 2026.5.26. "가져올 수 없음" 없음.
- 색인 수 28건 — 전일 대비 급락 없음(안정).
- 비고: sitemap 마지막 읽음 5.26 이후 재페치 없음. 페이지 보고서 데이터도 6.12 고정 — GSC 측 데이터 갱신 지연 지속.

### 개발 세션 인계 사항 (코드 영역)
- `best-markdown-editors-2026.html` 리디렉션 오류 해소 필요. 참조 페이지가 `/blog/...`(언어 프리픽스·.html 없음)과 `/ko/blog/...html` 두 형태로 잡혀 라우팅/리디렉션 불일치 의심. 정적 HTML 경로와 canonical/리디렉션 규칙 점검 권장.

### ✅ 개발 세션 처리 결과 (2026-06-27, 코드 영역 — Claude)
- **근본원인 = 리디렉션 오류 아님 → 영문 중복 페이지 + hreflang 충돌**로 확인.
  - 라이브 검사: `/blog/X`·`/ko/blog/X` 모두 단일 308/200으로 정상(루프 없음). "리디렉션 오류"는 GSC 과거 크롤(데이터 6.12 고정) 잔재.
  - 실제 문제: 영문판이 `/blog/`(6편)와 `/en/blog/`(10편)에 중복 존재(둘 다 self-canonical), sitemap 양쪽 등재, ko·ja hreflang 상호참조 깨짐.
- **조치(관리자 승인 — /en/blog/로 통일):**
  - 중복 `/blog/` 6편 삭제 + `_redirects`로 `/blog/* → /en/blog/:splat 301`. 라이브 301→200 검증 완료.
  - sitemap `/blog/` 7개 제거(→ url 41개), 기존 truncation 잔재(중복 `</urlset>`+orphan `/feedback/`) 복구.
  - ko·ja hreflang en·x-default `/blog/`→`/en/blog/`, og:url·mainEntityOfPage가 영문루트 가리키던 버그→자기 `/ko|ja/blog/`로 교정.
  - 내부 EN 스위처·legal 푸터 18곳 `/blog/`→`/en/blog/` 직접연결. IndexNow 재제출(200).
  - 커밋 083ce47.
